# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Working clones of the apps this directory publishes.
#
# They are their own repositories — cloned here so everything the portfolio deploys sits
# in one place locally, and ignored so this repo never swallows them. `apps.json` is the
# manifest and is tracked; this is just where the checkouts live.
apps/
.visual/
